import { serviceData } from "../components/data";
import WorkList from "../components/WorkList/WorkList";

const Service = () => {
  return <WorkList items={serviceData} />;
};

export default Service;
